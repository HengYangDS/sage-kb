# SAGE Knowledge Base - Conda Environment Configuration
# Generated: 2025-11-29
# Python: 3.12+

name: sage-kb

channels:
  - conda-forge
  - defaults

dependencies:
  # Python version
  - python>=3.12

  # Core dependencies
  - pyyaml>=6.0
  - pydantic>=2.0
  - typer>=0.9.0
  - rich>=13.0
  - structlog>=24.0

  # MCP dependencies
  - httpx>=0.25
  - uvicorn>=0.22

  # Development dependencies
  - pytest>=7.0
  - pytest-asyncio>=0.21
  - pytest-cov>=4.0
  - ruff>=0.1
  - mypy>=1.0

  # pip-only packages (not available in conda)
  - pip
  - pip:
      - mcp>=1.0
